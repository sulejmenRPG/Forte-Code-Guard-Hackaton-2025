# Быстрый старт за 5 минут ⚡

## Шаг 1: Установка (2 минуты)

### Windows:
```bash
# Запустите setup.bat
setup.bat
```

### Linux/Mac:
```bash
# Сделайте скрипт исполняемым и запустите
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Создаст файл `.env`

## Шаг 2: Настройка API ключей (2 минуты)

Откройте файл `.env` и заполните:

### Минимальная конфигурация (только OpenAI):
```env
GITLAB_TOKEN=your_gitlab_token_here
OPENAI_API_KEY=your_openai_key_here
WEBHOOK_SECRET=any_secret_phrase_here
```

### Где взять токены:

#### GitLab Token:
1. GitLab → Settings → Access Tokens
2. Создайте с правами: `api`, `read_repository`, `write_repository`
3. Скопируйте в `.env`

#### OpenAI API Key:
1. [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Create new secret key
3. Скопируйте в `.env`

**Альтернатива (бесплатно) - Gemini:**
```env
LLM_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_key_here
```
Получить: [makersuite.google.com](https://makersuite.google.com/)

## Шаг 3: Запуск сервера (30 секунд)

### Windows:
```bash
run.bat
```

### Linux/Mac:
```bash
chmod +x run.sh
./run.sh
```

Сервер запустится на `http://localhost:8000`

Откройте в браузере - должны увидеть:
```json
{
  "status": "healthy",
  "message": "AI Code Review Assistant is running",
  "version": "1.0.0"
}
```

## Шаг 4: Тестирование (30 секунд)

### Тест 1: Health Check
Откройте: `http://localhost:8000/health`

Должно вернуть: `{"status":"healthy"}`

### Тест 2: Проверка логов
В консоли должны видеть:
```
✅ Database initialized
✅ GitLab client initialized
✅ Code Analyzer initialized
🎯 LLM Provider: openai
🌐 Server running on 0.0.0.0:8000
```

## Шаг 5: Настройка GitLab (опционально для локального теста)

### Вариант A: С ngrok (для локального теста)

1. Скачайте [ngrok](https://ngrok.com/)
2. Запустите:
```bash
ngrok http 8000
```
3. Скопируйте URL (например: `https://abc123.ngrok.io`)

### Настройка webhook в GitLab:
1. Ваш проект → Settings → Webhooks
2. URL: `https://your-ngrok-url.ngrok.io/webhook/gitlab`
3. Secret Token: значение из `.env` (WEBHOOK_SECRET)
4. Trigger: ✅ Merge request events
5. Add webhook

### Вариант B: Без ngrok (просто тестируем API)

Можете использовать curl для теста:
```bash
curl http://localhost:8000/health
```

## Готово! 🎉

Теперь система готова к работе!

### Что дальше?

1. **Создайте тестовый MR в GitLab** - AI автоматически проанализирует
2. **Проверьте комментарии** - они появятся в MR через 2-3 минуты
3. **Изучите документацию** - читайте `SETUP_GUIDE.md` для деталей

## Возможные проблемы

### "ModuleNotFoundError"
```bash
# Убедитесь что активировано виртуальное окружение
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Переустановите зависимости
pip install -r requirements.txt
```

### "Invalid API key"
- Проверьте что API ключ правильно скопирован в `.env`
- Проверьте что нет лишних пробелов
- Для OpenAI проверьте баланс на аккаунте

### "Connection refused" от GitLab
- Проверьте GITLAB_TOKEN в `.env`
- Проверьте права токена (нужны: api, read_repository)

### Webhook не работает
- Убедитесь что ngrok запущен
- Проверьте URL в GitLab webhook
- Проверьте что WEBHOOK_SECRET одинаковый в `.env` и GitLab

## Дополнительные команды

### Остановить сервер:
`Ctrl + C`

### Просмотреть логи:
Все логи выводятся в консоль

### Запустить тесты:
```bash
pytest tests/
```

### Проверить версии:
```bash
python --version  # должен быть 3.11+
pip list  # все установленные пакеты
```

## Поддержка

Если что-то не работает:
1. Проверьте все шаги выше
2. Читайте `SETUP_GUIDE.md` для деталей
3. Проверьте логи в консоли
4. Проверьте файлы в `docs/` для архитектуры

Удачи! 🚀
