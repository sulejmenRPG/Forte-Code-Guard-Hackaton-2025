# 🗄️ Подключение PostgreSQL базы данных на Railway

## 🎯 Зачем нужно?

**Без базы данных:**
- ❌ Данные НЕ сохраняются
- ❌ Dashboard показывает mock данные
- ❌ Нет истории анализов
- ❌ Нет статистики

**С базой данных:**
- ✅ Все анализы сохраняются
- ✅ Dashboard показывает реальные данные
- ✅ История всех MR
- ✅ Статистика по команде
- ✅ ROI расчёты

---

## 🚀 Пошаговая инструкция

### **ШАГ 1: Добавить PostgreSQL на Railway**

1. **Открой Railway Dashboard:**
   ```
   https://railway.app/project/твой-проект-id
   ```

2. **Нажми "New" (справа вверху)**

3. **Выбери "Database":**
   ```
   New → Database → Add PostgreSQL
   ```

4. **PostgreSQL создастся автоматически** (~30 секунд)
   ```
   ✅ PostgreSQL 15 Ready
   ```

---

### **ШАГ 2: Получить DATABASE_URL**

1. **Открой PostgreSQL service** (нажми на него)

2. **Перейди в "Variables"**

3. **Найди `DATABASE_URL`:**
   ```
   DATABASE_URL
   postgresql://postgres:xxxxxxxxxxx@containers-us-west-xxx.railway.app:7432/railway
   ```

4. **Скопируй ВЕСЬ URL** (нажми на значок копирования)

---

### **ШАГ 3: Добавить DATABASE_URL в Backend**

1. **Открой Backend service** (web-production-...)

2. **Перейди в "Variables"**

3. **Нажми "Raw Editor"** (справа вверху)

4. **Добавь новую строку:**
   ```
   DATABASE_URL=postgresql://postgres:xxxxxxxxxxx@containers-us-west-xxx.railway.app:7432/railway
   ```
   *(вставь свой URL из шага 2)*

5. **Нажми "Update Variables"**

6. **Backend автоматически перезапустится!** (~2 минуты)

---

### **ШАГ 4: Проверить что работает**

**Через 2 минуты после перезапуска:**

1. **Открой логи Backend:**
   ```
   Backend service → Deployments → Latest → View Logs
   ```

2. **Найди в логах:**
   ```
   ✅ Database initialized
   ✅ Code Analyzer initialized
   🎯 LLM Provider: gemini
   🌐 Server running on 0.0.0.0:8080
   ```

3. **Если видишь это** → база подключена! ✅

---

### **ШАГ 5: Создать таблицы и тестовые данные**

**Вариант А: Автоматически (при первом запуске)**
```
Backend автоматически создаст таблицы при старте
Но они будут пустые!
```

**Вариант Б: С тестовыми данными (локально)**

1. **Скопируй DATABASE_URL из Railway**

2. **Локально установи:**
   ```bash
   export DATABASE_URL=postgresql://postgres:xxx@containers-us-west-xxx.railway.app:7432/railway
   ```

3. **Запусти скрипт инициализации:**
   ```bash
   python init_database.py
   ```

4. **Увидишь:**
   ```
   🚀 Initializing database...
   ✅ Database initialized!
   📝 Creating sample data...
   ✅ Saved review for MR #5
   ✅ Saved review for MR #4
   ✅ Saved review for MR #3
   ✅ Database setup complete!
   ```

---

### **ШАГ 6: Настроить Dashboard для реальных данных**

**В Streamlit Cloud:**

1. **Открой https://share.streamlit.io**

2. **Найди своё приложение** → **Manage App**

3. **Settings → Secrets**

4. **Edit Secrets → Вставь:**
   ```toml
   API_URL="https://web-production-d3b24.up.railway.app"
   ```
   *(замени на свой Railway URL)*

5. **Save**

6. **Reboot app** (красная кнопка)

---

### **ШАГ 7: Проверить Dashboard**

**Через 1-2 минуты:**

1. **Открой Dashboard:**
   ```
   https://forte-code-guard-hackaton-2025.streamlit.app
   ```

2. **Должен быть индикатор:**
   ```
   📡 Отображаются реальные данные из backend
   ```

3. **Если видишь:**
   ```
   🎨 Отображаются демо-данные (backend недоступен)
   ```
   → Проверь API_URL в Secrets!

---

## 🔍 Troubleshooting

### **Проблема 1: "Database not initialized"**

**В логах Railway:**
```
Database not initialized, skipping save
```

**Решение:**
```bash
# Проверь что DATABASE_URL правильный
# Должен начинаться с postgresql://
```

---

### **Проблема 2: "Connection refused"**

**Ошибка:**
```
connection to server at "..." port 5432 failed: Connection refused
```

**Решение:**
```
1. Проверь что PostgreSQL service запущен (Railway)
2. Проверь что URL скопирован полностью
3. Перезапусти Backend service
```

---

### **Проблема 3: Dashboard показывает demo данные**

**Причина:**
```
Dashboard не может подключиться к backend API
```

**Решение:**
```
1. Проверь API_URL в Streamlit Secrets
2. Убедись что backend работает:
   https://твой-backend.railway.app/health
3. Reboot dashboard app
```

---

## 📊 Проверка данных

### **Проверить что данные сохраняются:**

1. **Создай тестовый MR в GitLab:**
   ```
   - Отредактируй файл config.py
   - Commit → Create MR
   ```

2. **Жди 30-60 секунд** (AI анализирует)

3. **Открой Dashboard** → должны обновиться цифры:
   ```
   Проверено MR: 1 → 2
   AI Комментариев: 3 → 6
   ```

4. **Проверь backend API:**
   ```bash
   curl https://твой-backend.railway.app/stats
   ```

   Должен вернуть:
   ```json
   {
     "total_mrs": 2,
     "total_comments": 6,
     "time_saved_hours": 1.5,
     ...
   }
   ```

---

## 🎯 Итоговый чеклист

```
☐ PostgreSQL добавлен на Railway
☐ DATABASE_URL скопирован
☐ DATABASE_URL добавлен в Backend variables
☐ Backend перезапущен
☐ В логах видно "Database initialized"
☐ (Опционально) Запущен init_database.py
☐ API_URL добавлен в Streamlit Secrets
☐ Dashboard перезапущен
☐ Dashboard показывает "реальные данные"
☐ Создан тестовый MR для проверки
☐ Данные обновились в Dashboard
```

---

## 💡 Полезные команды

### **Проверить подключение к БД (локально):**
```bash
# Установи DATABASE_URL
export DATABASE_URL=postgresql://...

# Проверь подключение
psql $DATABASE_URL -c "SELECT 1"

# Посмотри таблицы
psql $DATABASE_URL -c "\dt"
```

### **Очистить данные:**
```bash
psql $DATABASE_URL -c "DROP TABLE IF EXISTS reviews CASCADE"
```

### **Посмотреть данные:**
```bash
psql $DATABASE_URL -c "SELECT * FROM reviews LIMIT 10"
```

---

## 🔐 Безопасность

**⚠️ ВАЖНО:**
```
❌ НЕ коммить DATABASE_URL в Git!
❌ НЕ публиковать URL в открытом доступе!
✅ Использовать только через environment variables
✅ Railway автоматически ротирует пароли
```

---

## 📚 Дополнительная информация

**Railway PostgreSQL документация:**
- https://docs.railway.app/databases/postgresql

**Структура базы данных:**
```sql
CREATE TABLE reviews (
    id SERIAL PRIMARY KEY,
    project_id INTEGER NOT NULL,
    mr_id INTEGER NOT NULL,
    mr_title TEXT,
    author TEXT,
    score FLOAT,
    total_issues INTEGER,
    critical_issues INTEGER,
    time_saved_minutes INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);
```

**Backend API endpoints:**
```
GET /stats           - Статистика (для dashboard)
GET /health          - Healthcheck
POST /webhook/gitlab - Webhook от GitLab
POST /api/feedback   - Feedback от сеньоров
```

---

**Готово! 🎉**

После настройки БД твой Dashboard будет показывать:
- ✅ Реальное количество проверенных MR
- ✅ Реальные комментарии AI
- ✅ Реальную экономию времени
- ✅ Статистику по команде
- ✅ Графики активности

**Идеально для презентации!** 🚀
