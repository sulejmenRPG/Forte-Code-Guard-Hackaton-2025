# Руководство по установке и настройке

## Шаг 1: Установка зависимостей

### Python и виртуальное окружение

```bash
# Убедитесь что у вас Python 3.11+
python --version

# Создайте виртуальное окружение
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Активируйте (Linux/Mac)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка переменных окружения

### Создайте файл `.env`

```bash
cp .env.example .env
```

### Заполните необходимые поля:

#### 1. GitLab Token

1. Зайдите в GitLab
2. User Settings → Access Tokens
3. Создайте токен с правами: `api`, `read_repository`, `write_repository`
4. Скопируйте токен в `.env`:

```env
GITLAB_TOKEN=glpat-your_token_here
```

#### 2. OpenAI API Key

1. Зайдите на [platform.openai.com](https://platform.openai.com/)
2. API keys → Create new secret key
3. Скопируйте в `.env`:

```env
OPENAI_API_KEY=sk-your_key_here
```

**Альтернатива - Gemini (бесплатно):**

1. Зайдите на [makersuite.google.com](https://makersuite.google.com/)
2. Get API key
3. В `.env`:

```env
LLM_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_key_here
```

#### 3. Webhook Secret

Придумайте секретную фразу:

```env
WEBHOOK_SECRET=my_super_secret_password_123
```

## Шаг 3: Настройка базы данных (опционально)

### Если у вас есть PostgreSQL:

```env
DATABASE_URL=postgresql://username:password@localhost:5432/code_review_db
```

### Если нет PostgreSQL:

Можно пока работать без БД, она нужна только для статистики.

## Шаг 4: Запуск сервера

```bash
cd backend
python main.py
```

Или через uvicorn:

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер запустится на `http://localhost:8000`

Проверьте: откройте в браузере `http://localhost:8000` - должно показать статус.

## Шаг 5: Настройка ngrok (для тестирования)

Чтобы GitLab мог отправлять webhook'и на ваш локальный компьютер:

### Установите ngrok:

1. Скачайте с [ngrok.com](https://ngrok.com/)
2. Распакуйте и положите в удобное место
3. Запустите:

```bash
ngrok http 8000
```

4. Вы получите URL типа: `https://abcd-1234.ngrok.io`
5. Этот URL используйте для webhook в GitLab

## Шаг 6: Настройка GitLab Webhook

### В вашем GitLab проекте:

1. Settings → Webhooks
2. URL: `https://your-ngrok-url.ngrok.io/webhook/gitlab`
3. Secret Token: скопируйте из `.env` (WEBHOOK_SECRET)
4. Trigger: отметьте `Merge request events`
5. SSL verification: можно отключить для ngrok
6. Add webhook

### Протестируйте webhook:

1. Нажмите "Test" → "Merge request events"
2. Должен прийти HTTP 200
3. В логах сервера увидите сообщение

## Шаг 7: Создайте тестовый Merge Request

1. Создайте новую ветку в GitLab
2. Сделайте изменения в коде (добавьте файл или измените)
3. Создайте Merge Request
4. Через 1-3 минуты должны появиться комментарии от AI

## Проверка работы

### 1. Health Check

```bash
curl http://localhost:8000/health
```

Должно вернуть: `{"status":"healthy",...}`

### 2. Логи сервера

При получении webhook должны увидеть:

```
📨 Received GitLab webhook
🔍 Processing MR #123 in project 456
🤖 Starting AI analysis...
✅ Analysis complete! Score: 8.5/10
```

## Troubleshooting

### Ошибка: "Invalid webhook token"

- Проверьте что WEBHOOK_SECRET в `.env` совпадает с GitLab

### Ошибка: "OpenAI API error"

- Проверьте что API key правильный
- Проверьте баланс на аккаунте OpenAI
- Попробуйте переключиться на Gemini

### Ошибка: "Failed to connect to GitLab"

- Проверьте GITLAB_TOKEN
- Проверьте права токена (нужны api, read_repository)

### Webhook не приходит

- Проверьте что ngrok запущен
- Проверьте URL в GitLab webhook
- Проверьте логи GitLab webhook (есть история попыток)

## Продакшен деплой

Для реального использования в банке:

### Вариант 1: Docker

```bash
# Создайте Dockerfile
docker build -t ai-code-review .
docker run -p 8000:8000 --env-file .env ai-code-review
```

### Вариант 2: Linux сервер

```bash
# Установите на сервер
# Используйте systemd для автозапуска
# Настройте nginx как reverse proxy
```

### Вариант 3: Cloud (AWS/Azure/GCP)

- Задеплойте через Cloud Run / ECS / App Service
- Настройте домен
- Настройте SSL сертификат
