# 🎨 Dashboard & Learning System Guide

## 🚀 Быстрый старт

### 1. Запуск Dashboard

```bash
# В отдельном терминале
streamlit run dashboard.py
```

Откроется в браузере: **http://localhost:8501**

---

## 📊 Функции Dashboard

### Analytics (📊)
- **KPI метрики**: Total MRs, Comments, Time Saved, Avg Score
- **Графики активности**: динамика по дням
- **Распределение проблем**: по категориям
- **Недавняя активность**: последние MR

### Settings (⚙️)
- **AI Configuration**: выбор модели, настройки
- **Integrations**: статус webhook, GitLab
- **Review Rules**: генератор `.codereview-rules.yaml`

### Team Performance (👥)
- **Статистика разработчиков**: рейтинг, score, MRs
- **ROI калькулятор**: сколько денег сэкономлено
- **Графики**: по score и времени

### Learning (🧠)
- **Feedback статистика**: 👍/👎
- **Learned patterns**: что AI запомнил
- **История обратной связи**

---

## 🧠 Learning система

### Как работает:

1. **AI оставляет комментарий** в GitLab MR
2. **Сеньор дает feedback**:
   - ✅ Правильно (reinforcement)
   - ❌ Неправильно (с объяснением)
3. **Система запоминает** в `data/learning_patterns.json`
4. **При следующем анализе** AI учитывает feedback

### API Endpoints:

```bash
# Добавить feedback
POST /api/feedback
{
  "comment_id": "123",
  "mr_id": 1,
  "project_id": 76260348,
  "feedback_type": "negative",
  "reason": "ORM is used, prepared statements not relevant",
  "senior_name": "alex_senior",
  "ai_comment": "Use prepared statements"
}

# Получить статистику
GET /api/feedback/stats

# Получить learned patterns
GET /api/learning/patterns
```

---

## 📋 .codereview-rules.yaml

### Где размещать:
**В корне GitLab репозитория** - AI автоматически прочитает при анализе MR.

### Пример создания:

1. **Через Dashboard**:
   - Settings → Review Rules
   - Заполни форму
   - Нажми "Generate .codereview-rules.yaml"
   - Скопируй в GitLab

2. **Вручную**:
   - Скопируй `.codereview-rules.yaml` из проекта
   - Адаптируй под свой проект
   - Закоммить в GitLab

### Структура:
```yaml
project_context:
  name: "Your Project"
  tech_stack: ["Python", "FastAPI"]

security_rules:
  - "No hardcoded secrets"
  - "SQL injection protection"

banking_requirements:
  - "Transaction logging"
  - "PCI DSS compliance"
```

---

## 💾 Структура данных

```
data/
├── stats.json              # Статистика для dashboard
├── feedback.json           # Feedback от сеньоров
└── learning_patterns.json  # Выученные паттерны
```

### Обновление статистики:

Dashboard читает `data/stats.json`. Чтобы обновить:

1. **Вручную**:
```json
{
  "total_mrs": 20,
  "total_comments": 85,
  "time_saved_hours": 8.5,
  "avg_score": 7.8
}
```

2. **Автоматически** (TODO):
   - Backend будет обновлять после каждого MR
   - Реальные данные из GitLab API

---

## 🎯 Для демо на хакатоне

### Что показать:

1. **Dashboard в действии**:
   - Красивые графики
   - Метрики ROI
   - Рейтинг команды

2. **Learning систему**:
   - Показать feedback процесс
   - Продемонстрировать learned patterns
   - Объяснить как AI учится

3. **Настройки**:
   - Генератор `.codereview-rules.yaml`
   - Выбор AI модели
   - Webhook статус

### Преимущества перед конкурентами:

✅ **Не просто AI комментарии** - полноценный dashboard для бизнеса  
✅ **Learning система** - AI становится умнее со временем  
✅ **ROI метрики** - показываем реальную экономию  
✅ **Enterprise-ready** - готов для использования в банке  

---

## 🐛 Troubleshooting

### Dashboard не запускается:
```bash
# Проверь установлен ли streamlit
pip install streamlit plotly pandas

# Проверь порт
netstat -ano | findstr :8501
```

### Нет данных в dashboard:
- Проверь существует ли `data/stats.json`
- Dashboard создаст mock данные если файла нет

### Learning не работает:
- Проверь `backend/feedback.py` импортируется
- Проверь права на запись в `data/`

---

## 📝 TODO для production:

- [ ] Подключить реальную БД вместо JSON
- [ ] Автообновление статистики после каждого MR
- [ ] WebSocket для real-time updates
- [ ] Аутентификация для dashboard
- [ ] Export отчетов в PDF
- [ ] Fine-tuning AI модели на feedback

---

**Готово к демо! 🚀**
