# Первый запуск и тестирование 🚀

## Шаг за шагом - ваш первый успешный запуск

### ✅ Шаг 1: Установка (5 минут)

```bash
# Откройте терминал в папке проекта
cd "C:\Users\Dias\Desktop\Hackaton Forte"

# Запустите автоматическую установку
setup.bat
```

Скрипт создаст виртуальное окружение и установит все зависимости.

---

### ✅ Шаг 2: Получение API ключей (10 минут)

#### 2.1 GitLab Personal Access Token

1. Откройте GitLab: https://gitlab.com
2. Нажмите на аватар → **Settings**
3. Слева выберите **Access Tokens**
4. Создайте новый токен:
   - **Name:** `ai-code-review-bot`
   - **Scopes:** отметьте:
     - ✅ `api`
     - ✅ `read_api`
     - ✅ `read_repository`
     - ✅ `write_repository`
   - **Expiration date:** 2026-01-01 (на год вперёд)
5. Нажмите **Create personal access token**
6. **ВАЖНО:** Скопируйте токен СЕЙЧАС (он больше не покажется)

#### 2.2 OpenAI API Key

**Вариант A - OpenAI (платный, но лучшее качество):**

1. Откройте: https://platform.openai.com/api-keys
2. Нажмите **Create new secret key**
3. Name: `ai-code-review`
4. Скопируйте ключ
5. Пополните баланс на $5-10 (хватит на весь хакатон)

**Вариант Б - Google Gemini (БЕСПЛАТНО):**

1. Откройте: https://makersuite.google.com/app/apikey
2. Нажмите **Get API key**
3. Создайте новый ключ
4. Скопируйте ключ

Рекомендую Gemini для начала - он бесплатный!

---

### ✅ Шаг 3: Настройка .env файла (2 минуты)

Откройте файл `.env` (он уже создан) в блокноте и заполните:

```env
# GitLab Configuration
GITLAB_URL=https://gitlab.com
GITLAB_TOKEN=glpat-ВСТАВЬТЕ_ВАШ_GITLAB_TOKEN

# LLM Provider - выберите один
LLM_PROVIDER=gemini

# Если выбрали OpenAI:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-ВСТАВЬТЕ_ВАШ_OPENAI_KEY

# Если выбрали Gemini (РЕКОМЕНДУЕТСЯ для начала):
GEMINI_API_KEY=ВСТАВЬТЕ_ВАШ_GEMINI_KEY

# Webhook Security - любая секретная фраза
WEBHOOK_SECRET=my_super_secret_password_12345

# Database (опционально, можно оставить как есть)
DATABASE_URL=sqlite:///./code_review.db

# Application Settings
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=True
```

**Сохраните файл!**

---

### ✅ Шаг 4: Первый запуск сервера (1 минута)

```bash
# Запустите сервер
run.bat
```

Вы должны увидеть:
```
🚀 Starting AI Code Review Assistant...
✅ Database initialized
✅ GitLab client initialized
✅ Code Analyzer initialized
🎯 LLM Provider: gemini
🌐 Server running on 0.0.0.0:8000
```

**Сервер работает!** 🎉

---

### ✅ Шаг 5: Тест в браузере (30 секунд)

Откройте браузер и перейдите на:

```
http://localhost:8000
```

Должны увидеть:
```json
{
  "status": "healthy",
  "message": "AI Code Review Assistant is running",
  "version": "1.0.0"
}
```

Также попробуйте:
```
http://localhost:8000/docs
```

Откроется Swagger UI с документацией API 📚

---

### ✅ Шаг 6: Создание тестового GitLab проекта (5 минут)

#### 6.1 Создайте новый проект

1. Зайдите на GitLab: https://gitlab.com
2. Нажмите **New project** → **Create blank project**
3. Заполните:
   - **Project name:** `test-code-review`
   - **Visibility:** Private (или Public)
4. Нажмите **Create project**

#### 6.2 Добавьте тестовый код

1. В проекте нажмите **+** → **New file**
2. Filename: `app.py`
3. Вставьте плохой код:

```python
import sqlite3

def get_user(user_id):
    conn = sqlite3.connect('users.db')
    cursor = conn.cursor()
    
    # SQL Injection vulnerability
    query = f"SELECT * FROM users WHERE id = {user_id}"
    cursor.execute(query)
    
    return cursor.fetchone()

# Hardcoded password
DB_PASSWORD = "admin123"

def process_payment(amount, account_id):
    # No error handling
    # No validation
    conn = sqlite3.connect('db.sqlite')
    cursor = conn.cursor()
    cursor.execute(f"UPDATE accounts SET balance = balance - {amount} WHERE id = {account_id}")
    conn.commit()
```

4. Commit message: `Add user functions`
5. Target branch: `main`
6. Нажмите **Commit changes**

---

### ✅ Шаг 7: Настройка Webhook (3 минуты)

#### 7.1 Установите ngrok (для локального теста)

1. Скачайте: https://ngrok.com/download
2. Распакуйте в удобное место
3. Откройте **новый** терминал и запустите:

```bash
ngrok http 8000
```

4. Скопируйте URL (например: `https://abc123.ngrok-free.app`)

#### 7.2 Настройте webhook в GitLab

1. В вашем проекте на GitLab
2. Settings → **Webhooks**
3. Заполните:
   - **URL:** `https://ваш-ngrok-url.ngrok-free.app/webhook/gitlab`
   - **Secret token:** скопируйте из `.env` (WEBHOOK_SECRET)
   - **Trigger events:** отметьте только `Merge request events`
   - **SSL verification:** можно отключить (для ngrok)
4. Нажмите **Add webhook**

#### 7.3 Протестируйте webhook

1. Внизу страницы найдите ваш webhook
2. Нажмите **Test** → **Merge request events**
3. Должно вернуться: `HTTP 200` или сообщение о том что это не MR event

**Webhook работает!** ✅

---

### ✅ Шаг 8: Создайте первый Merge Request (2 минуты)

1. В GitLab проекте нажмите **+** → **New branch**
2. Branch name: `fix-security`
3. Нажмите **Create branch**

4. Переключитесь на эту ветку
5. Откройте файл `app.py`
6. Нажмите **Edit**
7. Добавьте ещё одну плохую функцию:

```python
# No logging for critical operation
def transfer_money(from_account, to_account, amount):
    process_payment(amount, from_account)
    # ... rest of code
```

8. Commit changes to `fix-security` branch

9. Вверху появится кнопка **Create merge request**
10. Нажмите её
11. Заполните:
    - **Title:** `Fix security issues`
    - **Description:** `Testing AI code review`
12. Нажмите **Create merge request**

---

### ✅ Шаг 9: Смотрите магию! ✨ (2-3 минуты)

После создания MR:

1. **В терминале сервера** увидите:
```
📨 Received GitLab webhook
🔍 Processing MR #1 in project 12345
🤖 Starting AI analysis...
✅ Analysis complete! Score: 6.5/10
💬 Posting analysis results to GitLab...
```

2. **Обновите страницу MR в браузере**

3. Должны появиться:
   - 🤖 Большой комментарий с общим резюме
   - 🔴 Комментарии к проблемным строкам
   - 🏷️ Метка `ai-needs-fixes`

**Работает!** 🎉🎉🎉

---

## 🎯 Что делать дальше?

### Улучшите код и протестируйте снова:

1. Исправьте проблемы в коде
2. Сделайте commit в ту же ветку
3. AI автоматически проверит новые изменения
4. Оценка должна повыситься!

### Попробуйте разные типы кода:

- SQL injection
- XSS уязвимости
- Неэффективные алгоритмы
- Отсутствие error handling
- Захардкоженные пароли

AI должен найти всё это!

---

## 🐛 Решение проблем

### Проблема: "Invalid API key"

**Решение:**
- Проверьте что ключ правильно скопирован в `.env`
- Убедитесь что нет лишних пробелов
- Для OpenAI проверьте баланс на счету

### Проблема: "Failed to connect to GitLab"

**Решение:**
- Проверьте GITLAB_TOKEN в `.env`
- Убедитесь что токен имеет все нужные права
- Попробуйте создать новый токен

### Проблема: Webhook не приходит

**Решение:**
- Убедитесь что ngrok запущен
- Проверьте URL в GitLab webhook настройках
- Посмотрите логи webhook в GitLab (Recent events)
- Проверьте что WEBHOOK_SECRET совпадает

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
# Активируйте виртуальное окружение
venv\Scripts\activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: LLM слишком медленный

**Решение:**
- Используйте gpt-4o-mini вместо gpt-4 (в `llm_provider.py`)
- Или переключитесь на Gemini (быстрее)
- Увеличьте ANALYSIS_TIMEOUT в `.env`

---

## 📊 Проверка что всё работает

Чеклист:
- [x] Сервер запускается без ошибок
- [x] `http://localhost:8000` возвращает JSON
- [x] Webhook настроен в GitLab
- [x] MR создан
- [x] AI анализ появился в комментариях
- [x] Оценка и рекомендации видны

**Если всё отмечено - поздравляю, система работает!** 🎊

---

## 🎬 Следующие шаги

1. **Изучите код:**
   - `backend/main.py` - точка входа
   - `backend/code_analyzer.py` - логика анализа
   - `backend/prompts.py` - промпты для LLM

2. **Настройте под себя:**
   - Измените промпты в `prompts.py`
   - Добавьте свои правила в `.codereview-rules.yaml`
   - Настройте scoring в `code_analyzer.py`

3. **Подготовьтесь к хакатону:**
   - Читайте `TODO.md`
   - Следуйте `CHECKLIST.md`
   - Записывайте видео по `VIDEO_SCRIPT.md`

---

## 🆘 Нужна помощь?

1. Читайте документацию в папке `docs/`
2. Проверьте `SETUP_GUIDE.md` для деталей
3. Смотрите примеры в `examples/`
4. Проверяйте логи в консоли

---

**Удачи! Ты справишься! 💪**
